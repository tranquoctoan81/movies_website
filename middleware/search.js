// const { connect, connection } = require('../app/models/database');
// function search(keyWord) {
//     connect();
//     connection.query("SELECT * FROM movie WHERE name = ?", keyWord, function (err, data, fields) {
//         if (err) {
//             return res.json('Từ khóa không tồn tại!')
//         } else {
//             return req.data = data
//             next()
//         }
//     });
// }

// module.exports = search;
